services:
  jekyll:
    image: jekyll/jekyll
    command: bundle exec jekyll serve --host 0.0.0.0
    ports:
      - 4000:4000
    volumes:
      - ./:/srv/jekyll
      - bundle:/usr/local/bundle
      - gem:/usr/gem

volumes:
  bundle:
  gem:
